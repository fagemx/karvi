# Karvi Issue Execution Plan — 2026-03-09 (updated)

## 進度總覽

### 已完成（Closed）

| # | Issue | PR | 完成方式 |
|---|-------|----|---------|
| **276** | template-based step prompts | #296 | opencode auto |
| **277** | retry-aware prompt | (merged earlier) | opencode auto |
| **279** | per-state concurrency limits | #299 | opencode auto |
| **288** | scope config passthrough | #297 | opencode auto |
| **298** | CLI bin entry + npx support | — (direct to main) | 手動 |
| **292** | worktree lifecycle + spawn failure handling | #293 | 手動 |
| **290** | runtime observability — stop blind dispatch | (part of #293) | 手動 |
| **286** | dispatch scope guard | (merged earlier) | opencode auto |
| **285** | step reset endpoint | (merged earlier) | opencode auto |
| **274** | cancelled task status | (merged earlier) | 手動 |
| **273** | smart idle detection | (merged earlier) | opencode auto |
| **268** | repo-resolver GitHub slug fix | #269 | opencode auto |
| **262** | runtime/skill/preflight discovery endpoints | #264, #265 | opencode auto |
| **260** | npm run go CLI | #266 | opencode auto |

### 仍然 Open

```
P0 — Kernel 核心能力
  #218  converge legacy dispatch into step pipeline
  #216  configurable step timeout（controls 已有，需驗證生效）
  #248  reopen task — resume completed with session continuation

P1 — 操作完整性
  #272  preflight check for npm run go
  #278  workspace lifecycle hooks (before_run / after_run)
  #213  scope lock — restrict agent file modifications
  #202  inject skill context + completion criteria
  #189  wave/batch grouping to prevent unintended auto-dispatch

P2 — 文件
  #259  troubleshooting guide
  #257  API usage examples with curl

P3 — 整合（有客戶需求再做）
  #270  GitHub @karvi webhook
  #271  Jira @karvi webhook
  #261  mobile create task form
  #192  repository registry — source.repo → localPath mapping
  #186  chronicle multi-tool session tracking

P4 — SaaS / Gateway（未上線，暫不排）
  #174  gateway rate-limit session verification
  #173  gateway serialize port allocation
  #172  gateway validate X-Forwarded-For
  #171  board optimistic locking
  #170  gateway rate limiting auth endpoints
  #169  encryption at rest
  #168  container sandbox
  #167  auto repo provisioning

P5 — Village 願景層（暫不排）
  #165  governance interaction panel
  #164  chief T2 actions
  #163  pluggable ChiefProfile
  #162  tool tier governance (T0-T4)
  #160  deliverable contract
  #150  nation strategic governance
```

## Dependency DAG（剩餘 P0-P1）

```
Independent (可並行)
  #216  verify step timeout
  #272  preflight check
  #278  workspace hooks
  #213  scope lock
  #202  inject skill context
  #189  wave/batch grouping

Depends on above
  #248  reopen task（需 #274 cancel 已完成 ✅）
  #218  converge legacy dispatch（需 step pipeline 完全可靠）
```

## 建議 Next Batch

**Batch A（可全並行，無依賴）：**

| # | Issue | 主要檔案 | 工作量 |
|---|-------|---------|--------|
| **216** | configurable step timeout | management.js, step-worker.js | 小 |
| **272** | preflight check | scripts/go.js | 小 |
| **248** | reopen task | routes/tasks.js | 中 |
| **202** | inject skill context | context-compiler.js | 小 |

```bash
npm run go -- 216 248 202 272 -y
```

**Batch B（Batch A 後）：**

| # | Issue | 依賴 |
|---|-------|------|
| **189** | wave/batch grouping | 獨立但需 P0 穩定 |
| **278** | workspace hooks | 獨立 |
| **218** | converge legacy dispatch | 所有 P0 完成後 |

## 建議關閉

| # | Issue | 理由 |
|---|-------|------|
| 148-150 | multi-village, territory, nation | 願景層，離落地太遠 |
| 160-165 | village governance 系列 | 同上 |
| 186 | chronicle multi-tool tracking | 無明確需求 |
| 167-174 | SaaS/gateway security | gateway 未上線 |
